[project]
name = "motorshop-e2e"
version = "0.1.0"
description = "Playwright E2E tests for Motor Shop"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "pytest-playwright>=0.5.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
